hibernate:
  jdbc:
    time_zone: UTC
  show_sql: true

spring:
  application:
    name: shopping-cart
  dataSource:
    driverClassName: org.postgresql.Driver
    password: 1qaz!QAZ
    url: jdbc:postgresql://localhost:6543/shopping-cart
    username: dbuser
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always